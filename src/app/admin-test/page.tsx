export default function AdminTestPage() {
  return (
    <main
      style={{
        minHeight: '100vh',
        background: '#000',
        color: '#fff',
        padding: 24,
        fontFamily: 'Arial, sans-serif',
      }}
    >
      <h1>ADMIN TEST OK</h1>
      <p>If you see this, the route itself works.</p>
    </main>
  )
}
